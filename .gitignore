__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local debug / screenshot artifacts
_*.png
_*.txt
_*.log

# Cross-section figures (regenerated: viewer 'I' hotkey + --section-xz)
section_view_*.png
section_xz.png

# Locally generated artefacts (regenerated from the pipeline)
deformed.stl
deformed.gcode
deformed_3d.gcode
result.gcode
propeller_inverse*.gcode
propeller_nonplanar*.gcode
propeller_conformed*.gcode
propeller_clipped*.gcode

# OS / non-ASCII-named screenshots
Снимок*.png
Скриншот*.png

# IDE
.idea/
.vscode/
*.swp

# Reference-only external slicer (cloned separately, not part of this repo)
S4_Slicer/

# Regenerable deformed-mesh artifacts (Z-only + full-3D comparison)
deformed_zonly.stl
deformed_3d.stl

# Local ad-hoc test parts (not pipeline fixtures)
gfkilox-case_top_r2.stl
