# Поверхности роста: как строятся сейчас и в чём проблема

Заметка по математике (без кода). Описывает **текущее** построение слой-поверхностей
(`vff/growth.py` + `vff/deform.py`, метод по умолчанию `depth-method="vectors"`),
интуитивную суть проблемы на «грибке» и её строгую формулировку.

---

## 0. Обозначения

- $\Omega \subset \mathbb{Z}^3$ — воксели модели (занятые); $\partial\Omega$ — её граница.
- $x_i \in \mathbb{R}^3$ — мировой центр вокселя $i$ (шаг сетки — `pitch`).
- $S = \{\, i \in \Omega : z_i = z_{\text{bed}}\,\}$ — **постель** (нижний занятый слой), сиды роста.
- $d_{ij} = j - i$ — целочисленное смещение между соседями по решётке (в вокселях).
- $\hat z = (0,0,1)$ — вертикаль; $\theta$ — `max_tilt` (предельный угол сопла от вертикали).
- $G_\sigma$ — гауссово сглаживание с $\sigma$ (в вокселях).

Поверхности в итоге — это **множества уровня** скалярного поля $\varphi$:
$$\Sigma_c \;=\; \{\, x : \tilde\varphi(x) = c \,\}.$$
Весь вопрос в том, **как строится $\varphi$**.

---

## 1. Как поверхности строятся сейчас

Конвейер из пяти шагов. Первичны **вектора**, поле $\varphi$ — вторично (подгоняется под них).

### 1.1. BFS-рост от постели → целочисленный шаг

26-связный BFS от $S$ вверх сквозь $\Omega$ даёт целое поле шага
$$s_i \in \{0,1,2,\dots\}, \qquad s_i\big|_{S}=0,$$
по сути — **геодезическое расстояние от постели внутри тела** (квантованное в шагах дилатации).

### 1.2. Сырые вектора роста (предшественники)

Для каждого вокселя $i$ берём его более «старых» соседей (с меньшим $s$) в **ближайшем
непустом классе** $\kappa_i\in\{1,2,3\}$ (грань / ребро / вершина, по $\lVert d\rVert^2$),
усредняем их смещения, нормируем и масштабируем к $\sqrt{\kappa_i}$, затем меняем знак
(чтобы вектор смотрел «старый → новый», т.е. вверх по росту):
$$
v_i^{\text{raw}} \;=\; -\,\sqrt{\kappa_i}\;\frac{\sum_{d\in\text{class}} d}{\bigl\lVert \sum_{d\in\text{class}} d\bigr\rVert},
\qquad \lVert v_i^{\text{raw}}\rVert \in \{1,\sqrt2,\sqrt3\}.
$$

### 1.3. Tilt-clamp (зажим к вертикали)

Каждый вектор, чей угол к $\hat z$ больше $\theta$, **поворачивается** в плоскости
$(v,\hat z)$ обратно до ровно $\theta$ (длина сохраняется):
$$
v_i \;=\; C_\theta\bigl(v_i^{\text{raw}}\bigr)
\;=\;
\begin{cases}
v_i^{\text{raw}}, & \angle(v_i^{\text{raw}},\hat z)\le\theta,\\[4pt]
\lVert v_i^{\text{raw}}\rVert\,\bigl(\sin\theta\,\hat h_i + \cos\theta\,\hat z\bigr), & \text{иначе,}
\end{cases}
$$
где $\hat h_i$ — единичная **горизонтальная** компонента направления $v_i^{\text{raw}}$.
Это поворот $R_i$, **зависящий от точки**: $v_i = R_i\, v_i^{\text{raw}}$.

### 1.4. Интегрирование векторов в потенциал $\varphi$ ⟵ ядро всего

Хотим поле $\varphi$, чьи разности вдоль рёбер совпадают с проекцией векторов на ребро:
$$
\varphi_j - \varphi_i \;\approx\; \bar v_{ij}\cdot d_{ij},
\qquad \bar v_{ij} = \tfrac12\,(v_i + v_j).
$$
Это переопределённая система (рёбер больше, чем узлов) → решаем **методом наименьших квадратов**:
$$
\boxed{\;\varphi \;=\; \arg\min_{\varphi}\;
\sum_{(i,j)\in E}\Bigl[(\varphi_j-\varphi_i) - \bar v_{ij}\cdot d_{ij}\Bigr]^2
\;+\;\underbrace{\sum_{i\in S}\beta\,\varphi_i^2}_{\text{пин постели }\varphi|_S=0}
\;+\;\varepsilon\lVert\varphi\rVert^2 \;}
$$
($E$ — рёбра 26-связности внутри $\Omega$; $\beta$ — большой штраф ≈ условие Дирихле;
$\varepsilon$ — крошечная регуляризация Тихонова).

Нормальные уравнения этой задачи — **дискретное уравнение Пуассона**:
$$
\bigl(L + \operatorname{diag}(\text{pen})\bigr)\,\varphi = b,
\qquad
L_{ii}=\deg(i),\;\; L_{ij}=-1,
\qquad
b_i = \sum_{j\sim i} \bar v_{ij}\cdot(x_i - x_j).
$$
$L$ — граф-лапласиан, $b$ — **дискретная дивергенция** целевого поля. В непрерывном пределе это ровно
$$
\min_\varphi \int_\Omega \lVert \nabla\varphi - v\rVert^2\,dV
\quad\Longleftrightarrow\quad
\Delta\varphi = \nabla\!\cdot v \ \text{ в }\Omega,\quad
\partial_n\varphi = v\!\cdot\! n \ \text{ на }\partial\Omega,\quad
\varphi|_S = 0.
$$
Решается сопряжёнными градиентами (система SPD).

### 1.5. Заполнение вне модели, сглаживание, масштаб

$$
\bar\varphi = G_\sigma * \Phi,\qquad
\Phi = \begin{cases}\varphi, & \text{внутри }\Omega,\\ \text{ближайшее внутреннее (extend)} \\ \text{или } k-k_{\text{bed}} \text{ (vertical, для виз.)}, & \text{вне }\Omega,\end{cases}
\qquad
\tilde\varphi = \bar\varphi\cdot\frac{N-1}{\max_\Omega\bar\varphi}.
$$

### 1.6. Сами поверхности

$$
\Sigma_c=\{x:\tilde\varphi(x)=c\},\qquad c=i+\tfrac12,\ \ i=0,\dots,N-1
$$
— извлекаются marching cubes. Деформация затем кладёт $z_{\text{new}}=\tilde\varphi\cdot dz + z_{\text{bed}}$
(каждая $\Sigma_c$ → горизонтальная плоскость).

### Что мы **хотим** (условие нормальности)

Чтобы «все вектора шага $n$ были нормальны к поверхности $n$», нужно
$$
v \parallel \nabla\varphi \quad\text{всюду}
\quad\Longleftrightarrow\quad
v=\nabla\varphi \ \text{для некоторого}\ \varphi
\quad\Longleftrightarrow\quad
\boxed{\;\nabla\times v = 0\;}\ \text{(поле потенциально)}.
$$
**Только** при безвихревом $v$ его множества уровня перпендикулярны $v$ точно.

---

## 2. Проблема «на пальцах»

Сначала строим стрелки (предшественники), потом **загибаем их к вертикали** (clamp),
а потом ищем поверхности, к которым стрелки были бы нормалями.

Но после загиба стрелки **уже не складываются** в склон какого-то одного «холма»:
нет ни одной системы поверхностей, для которой все они — нормали. Решатель берёт
**наилучший компромисс по среднему**, и на грибке этот компромисс плохой:

- стрелки в шляпке загнуты сильнее всего → именно там поверхности **не перпендикулярны** стрелкам;
- шляпка «питается» через узкую ножку → дальше всего от постели оказывается точка **внутри**
  шляпки → вокруг неё поля наматываются **замкнутыми кольцами**;
- поверхности **провисают вниз**, копируя вогнутость геометрии, вместо того чтобы быть
  максимально плоскими.

Коротко: мы интегрируем **неинтегрируемое** поле, и вся невязка садится ровно на проблемную геометрию.

---

## 3. Проблема математически

### 3.1. Нарушение нормальности = ротор зажатого поля

Clamp — это поворот $R_i$, **зависящий от точки**. Даже если бы $v^{\text{raw}}$ был
градиентом, $v = R(x)\,v^{\text{raw}}$ в общем случае **не градиент**:
$$
\nabla\times v \;\neq\; 0.
$$
Разложение Гельмгольца: $\;v=\nabla\varphi+\nabla\times A\;$ ($+$ гармоническая часть). Наименьшие
квадраты восстанавливают **только потенциальную проекцию** $\nabla\varphi$. Нормаль поверхности —
это $\nabla\varphi$, а не $v$:
$$
n_{\Sigma}=\frac{\nabla\varphi}{\lVert\nabla\varphi\rVert},
\qquad
\angle(v,\,n_\Sigma)\neq 0\ \text{там, где}\ \nabla\times v\neq 0 .
$$
Невязка $\;r=v-\nabla\varphi=\nabla\times A\;$ — это **потерянная вихревая часть**; её норма
$\lVert r\rVert$ максимальна там, где clamp поворачивал сильнее всего (низ/борт шляпки).
Вот **точная причина**, почему условие нормальности «почему-то не выполняется»: оно нарушается
ровно на величину ротора зажатого поля.

### 3.2. Замкнутые поверхности = внутренний максимум distance-поля (НЕ clamp)

$\varphi$ решает $\Delta\varphi=\nabla\!\cdot v$ — Пуассон с **источником** $\rho=\nabla\!\cdot v$.
Когда $\rho\not\equiv 0$, принцип максимума не действует и у $\varphi$ **могут быть внутренние
экстремумы**:
$$
\rho\not\equiv 0\ \Rightarrow\ \exists\ \text{внутренний максимум }x^\* .
$$
В точке максимума $x^\*$: $\nabla\varphi(x^\*)=0$ (направление роста **не определено**), а
соседние $\Sigma_c$ — **вложенные замкнутые оболочки**. Сравните с однородным случаем
$\rho\equiv0$ (гармоническое $\Delta\varphi=0$): принцип максимума ⇒ экстремумов внутри нет ⇒
уровни **никогда не замыкаются**.

**Откуда берётся $\rho\neq0$ — и это НЕ clamp.** Источник несёт **distance-структуру** роста:
$v\approx\nabla(\text{геодезическое расстояние от стола})$, а у расстояния через бутылочное
горлышко (узкая ножка → широкая шляпка) есть внутренний максимум на **cut locus** —
геодезически дальше всего отстоящие точки (кончики борта шляпки). Вокруг них $\Sigma_c$ и
замыкаются. Это свойство расстояния, а **не** поворота clamp.

> **Проверено эмпирически** (подсказка от ChatGPT, спасибо): если бы виноват был clamp,
> то при `--max-tilt 179` (зажима фактически нет) замыкания исчезли бы. Они **остаются**.
> Детектор внутренних 2D-экстремумов в срезе грибка (`np_test1`, питч 0.2) даёт:
>
> | поле | tilt 30 | tilt 179 |
> |---|---|---|
> | `vectors` | **2** центра замкнутых петель | **2** |
> | `harmonic` | **0** | **0** |
>
> Clamp **не меняет** число замыканий (2→2) — значит он их не причина. Более того, тяжёлый
> зажим (tilt 30) делает поле почти вертикально-монотонным и **маскирует** проблему по столбцам,
> но петли всё равно есть (вокруг горизонтальных максимумов на cut locus). `harmonic` даёт **0**
> при любом tilt — по принципу максимума. (`fmm`/`dijkstra` — то же расстояние, замыкание ещё
> прямее.)
>
> Итог: **clamp отвечает за §3.1 (нормальность), а НЕ за замкнутые поверхности.** Замыкания — это
> distance-природа поля; лечатся только сменой класса поля (гармоника), а не настройкой/снятием
> зажима.

### 3.3. Выпуклость вниз = наследование геометрии + сглаживание

Два вклада:

1. **Источник несёт форму геометрии.** $\rho=\nabla\!\cdot v$ построен из предшественников,
   т.е. отражает, где лежит материал; поэтому уровни $\varphi$ гнутся **вслед за телом**, а не по
   принципу гладкости. Distance-поле в принципе «жмётся к границе» и наследует её вогнутость.
2. **Сглаживание двигает уровни по кривизне.** $G_\sigma$ эквивалентно шагу теплопроводности
   $\partial_t\varphi=\Delta\varphi$ (при $t=\sigma^2/2$), т.е. поле сдвигается пропорционально
   $\Delta\varphi$. Вместе с `extend`-заполнением пустоты под козырьком это **углубляет провисание**
   вниз, а не выполаживает его.

---

## 4. Куда целиться (резюме направления)

Корень всех трёх пунктов один: **поле строится как «расстояние/предшественники + clamp + интеграл»**,
которое (а) неинтегрируемо (ломает нормальность) и (б) есть distance-поле (внутренние экстремумы и
наследование геометрии).

Чистая альтернатива — сделать $\varphi$ **первичным** через корректно поставленную эллиптическую
задачу, а направления определять как $\nabla\varphi$:
$$
\Delta\varphi=0\ \text{в}\ \Omega,\quad \varphi|_S=0,\quad \varphi|_{\text{top}}=\varphi_{\max},\quad \partial_n\varphi|_{\text{walls}}=0.
$$
Тогда **бесплатно**:

| Свойство | Почему |
|---|---|
| нормальность $v\!\perp\!\Sigma$ — точно | $v\equiv\nabla\varphi$ по определению |
| нет замкнутых поверхностей | принцип максимума: нет внутренних экстремумов |
| нет «выпуклости вниз» | гармоника минимизирует изгиб, не копирует геометрию |

Цена: задать «верх» как границу Дирихле; tilt-ограничение вносить **в само уравнение** (ограниченная
оптимизация $\lvert\angle(\nabla\varphi,\hat z)\rvert\le\theta$ по всему объёму), а не зажимом
постфактум. Физически непечатаемый низ козырька при этом честно остаётся overhang'ом — **без** отдельного
детекта, потому что правило одно и однородно по всему телу.
